apiVersion: v2
name: external-secrets
description: "Sync secrets from external providers — managed by K8sAgents Bootstrap"
version: 1.0.0
type: application
dependencies:
  - name: external-secrets
    version: "2.1.0"
    repository: "https://charts.external-secrets.io"
