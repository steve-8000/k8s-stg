apiVersion: v2
name: vault
description: "Secret management — dynamic secrets, encryption — managed by K8sAgents Bootstrap"
version: 1.0.0
type: application
dependencies:
  - name: vault
    version: "0.32.0"
    repository: "https://helm.releases.hashicorp.com"
