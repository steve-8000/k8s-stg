apiVersion: apps/v1
kind: Deployment
metadata:
  name: alice-trade
  namespace: alice-trade
  labels:
    app.kubernetes.io/name: alice-trade
    app.kubernetes.io/instance: alice-trade-stg
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app.kubernetes.io/name: alice-trade
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app.kubernetes.io/name: alice-trade
        app.kubernetes.io/instance: alice-trade-stg
    spec:
      containers:
        - name: alice-trade
          image: docker.io/library/alice-trade:v11
          imagePullPolicy: Never
          ports:
            - name: http
              containerPort: 3002
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
          resources:
            requests:
              cpu: 200m
              memory: 512Mi
            limits:
              cpu: "1"
              memory: 2Gi
          env:
            - name: NODE_ENV
              value: production
          volumeMounts:
            - name: config
              mountPath: /app/data/config
            - name: sessions
              mountPath: /app/data/sessions
            - name: brain
              mountPath: /app/data/brain
            - name: trading
              mountPath: /app/data/trading
            - name: news
              mountPath: /app/data/news
            - name: market-data
              mountPath: /app/data/market-data
            - name: logs
              mountPath: /app/logs
          securityContext:
            allowPrivilegeEscalation: false
      volumes:
        - name: config
          hostPath:
            path: /opt/alice-trade/config
            type: DirectoryOrCreate
        - name: sessions
          hostPath:
            path: /opt/alice-trade/sessions
            type: DirectoryOrCreate
        - name: brain
          hostPath:
            path: /opt/alice-trade/brain
            type: DirectoryOrCreate
        - name: trading
          hostPath:
            path: /opt/alice-trade/trading
            type: DirectoryOrCreate
        - name: news
          hostPath:
            path: /opt/alice-trade/news
            type: DirectoryOrCreate
        - name: market-data
          hostPath:
            path: /opt/alice-trade/market-data
            type: DirectoryOrCreate
        - name: logs
          hostPath:
            path: /opt/alice-trade/logs
            type: DirectoryOrCreate
