apiVersion: apps/v1
kind: Deployment
metadata:
  name: state-engine
  namespace: polybase-trading
  labels:
    app.kubernetes.io/name: state-engine
    app.kubernetes.io/instance: state-engine-stg
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app.kubernetes.io/name: state-engine
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app.kubernetes.io/name: state-engine
        app.kubernetes.io/instance: state-engine-stg
    spec:
      containers:
        - name: state-engine
          image: ghcr.io/steve-8000/clab-polybase-binance/state-engine:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: APP_ENV
              value: prod
            - name: APP__NATS__URL
              value: "nats://nats.polybase-infra.svc.cluster.local:4222"
            - name: APP__REDIS__URL
              value: "redis://redis.polybase-infra.svc.cluster.local:6379"
            - name: APP__CLICKHOUSE__URL
              value: "http://clickhouse.polybase-infra.svc.cluster.local:8123"
            - name: APP__CLICKHOUSE__DATABASE
              value: trading
            - name: RUST_LOG
              value: info
          ports:
            - name: metrics
              containerPort: 9090
