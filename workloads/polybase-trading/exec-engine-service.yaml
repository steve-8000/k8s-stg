apiVersion: v1
kind: Service
metadata:
  name: exec-engine
  namespace: polybase-trading
  labels:
    app.kubernetes.io/name: exec-engine
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: exec-engine
  ports:
    - name: metrics
      port: 9090
      targetPort: metrics
