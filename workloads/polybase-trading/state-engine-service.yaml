apiVersion: v1
kind: Service
metadata:
  name: state-engine
  namespace: polybase-trading
  labels:
    app.kubernetes.io/name: state-engine
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: state-engine
  ports:
    - name: metrics
      port: 9090
      targetPort: metrics
