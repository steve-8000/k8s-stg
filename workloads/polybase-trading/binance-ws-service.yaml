apiVersion: v1
kind: Service
metadata:
  name: binance-ws
  namespace: polybase-trading
  labels:
    app.kubernetes.io/name: binance-ws
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: binance-ws
  ports:
    - name: metrics
      port: 9090
      targetPort: metrics
