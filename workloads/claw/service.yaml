apiVersion: v1
kind: Service
metadata:
  name: claw
  namespace: claw
  labels:
    app.kubernetes.io/name: claw
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: claw
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
