apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: clab-apps
  labels:
    app.kubernetes.io/name: user-service
spec:
  selector:
    app.kubernetes.io/name: user-service
  ports:
    - name: http
      port: 80
      targetPort: http
