apiVersion: v1
kind: Service
metadata:
  name: control-service
  namespace: clab-apps
  labels:
    app.kubernetes.io/name: control-service
spec:
  selector:
    app.kubernetes.io/name: control-service
  ports:
    - name: http
      port: 80
      targetPort: http
