apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: clab-apps
  labels:
    app.kubernetes.io/name: auth-service
spec:
  selector:
    app.kubernetes.io/name: auth-service
  ports:
    - name: http
      port: 80
      targetPort: http
