apiVersion: v1
kind: Service
metadata:
  name: timescaledb
  namespace: clab-data
  labels:
    app.kubernetes.io/name: timescaledb
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: timescaledb
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
