apiVersion: v1
kind: Service
metadata:
  name: realtime-gateway
  namespace: clab-core
  labels:
    app.kubernetes.io/name: realtime-gateway
spec:
  selector:
    app.kubernetes.io/name: realtime-gateway
  ports:
    - name: http
      port: 80
      targetPort: http
