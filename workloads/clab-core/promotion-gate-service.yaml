apiVersion: v1
kind: Service
metadata:
  name: promotion-gate
  namespace: clab-core
  labels:
    app.kubernetes.io/name: promotion-gate
spec:
  selector:
    app.kubernetes.io/name: promotion-gate
  ports:
    - name: http
      port: 80
      targetPort: http
