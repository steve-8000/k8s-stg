apiVersion: v1
kind: Service
metadata:
  name: positions-ledger
  namespace: clab-core
  labels:
    app.kubernetes.io/name: positions-ledger
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: positions-ledger
  ports:
    - name: http
      port: 80
      targetPort: http
