apiVersion: v1
kind: Service
metadata:
  name: proposal-service
  namespace: clab-core
  labels:
    app.kubernetes.io/name: proposal-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: proposal-service
  ports:
    - name: http
      port: 80
      targetPort: http
