apiVersion: v1
kind: Service
metadata:
  name: query-api
  namespace: clab-core
  labels:
    app.kubernetes.io/name: query-api
spec:
  selector:
    app.kubernetes.io/name: query-api
  ports:
    - name: http
      port: 80
      targetPort: http
