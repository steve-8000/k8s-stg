apiVersion: v1
kind: Service
metadata:
  name: execution-router
  namespace: clab-core
  labels:
    app.kubernetes.io/name: execution-router
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: execution-router
  ports:
    - name: http
      port: 80
      targetPort: http
