apiVersion: v1
kind: Service
metadata:
  name: risk-engine
  namespace: clab-core
  labels:
    app.kubernetes.io/name: risk-engine
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: risk-engine
  ports:
    - name: http
      port: 80
      targetPort: http
