apiVersion: v1
kind: Service
metadata:
  name: portfolio-canonical-state
  namespace: clab-core
  labels:
    app.kubernetes.io/name: portfolio-canonical-state
spec:
  selector:
    app.kubernetes.io/name: portfolio-canonical-state
  ports:
    - name: http
      port: 80
      targetPort: http
