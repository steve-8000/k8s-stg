apiVersion: v1
kind: Service
metadata:
  name: feature-pipeline
  namespace: clab-core
  labels:
    app.kubernetes.io/name: feature-pipeline
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: feature-pipeline
  ports:
    - name: http
      port: 80
      targetPort: http
